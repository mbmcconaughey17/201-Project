<!DOCTYPE html>
<html lang='en'>

<div class="row">
	<div class="text-center col-sm-8 col-sm-offset-2">
		<h1>Results Matching Your Search</h1>
	</div>
</div>

	<% @apps.each do |app| %>
<div class="row">
		<div class="app">
			<div class="col-sm-2 col-sm-offset-1">
			<%= link_to image_tag(app.ImageURL, :alt => "App Icon", :class => "img-rounded"), app_path(app) %>
		</div>
		<div class="col-xs-6">
  			<h3 class="app-name"><%= link_to app.name, app %></h3>
		</div>
	</div>
</div>
<br>
	<% end %>

</html>